<% if @vote.valid? %>
  $("#<%= vote_count_element_id(@obj) %>").html("<%= pluralize @obj.total_votes, 'vote' %>");
<% else %>
  alert("You can only vote once on a <%= @obj.class.name.downcase %>");
<% end %>
